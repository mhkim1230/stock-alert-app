# 클라우드 배포용 환경변수 설정 예시
# 이 파일을 참고하여 Render.com에서 환경변수를 설정하세요

# 데이터베이스 설정 (Supabase)
DATABASE_URL=postgresql://postgres:rlaalghk1!@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# 애플리케이션 설정
DEBUG=false
LOG_LEVEL=INFO

# 보안 설정
SECRET_KEY=your-secret-key-will-be-auto-generated-by-render

# 캐싱 및 성능 설정
ENABLE_CACHING=true
CACHE_TIMEOUT=300
MAX_RETRIES=3
REQUEST_TIMEOUT=10

# 파싱 최적화 (IP 차단 방지)
MIN_REQUEST_DELAY=1.0
MAX_REQUEST_DELAY=3.0

# APNs 설정 (iOS 푸시 알림) - 옵션
APNS_USE_SANDBOX=true
APNS_KEY_ID=your-apns-key-id
APNS_TEAM_ID=your-team-id
APNS_BUNDLE_ID=com.example.stockalert

# 주의사항:
# 1. DATABASE_URL에서 [YOUR-PROJECT-REF]를 실제 Supabase 프로젝트 ID로 변경
# 2. Render.com에서는 SECRET_KEY를 자동 생성으로 설정
# 3. 이 파일은 참고용이며 실제 설정은 Render.com 대시보드에서 진행 